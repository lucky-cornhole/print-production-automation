document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;document.querySelectorAll('.job-card').forEach(c=>{let v=true;if(f==='single')v=c.dataset.type==='single'&&c.dataset.custom!=='yes';if(f==='double')v=c.dataset.type==='double'&&c.dataset.custom!=='yes';if(f==='custom')v=c.dataset.custom==='yes';if(f==='ready')v=c.dataset.ready==='yes';if(f==='unverified')v=!c.dataset.decision;if(f==='wrong')v=c.dataset.decision==='WRONG'||c.dataset.status==='REVIEW'||c.dataset.status==='MISSING';c.style.display=v?'':'none'})}));
